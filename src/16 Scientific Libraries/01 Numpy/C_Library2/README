This example creates a C/C++ extension module.
The shared library is built using CMake and pybind11

1) First install pybind11
    pip install pybind11

2) Compile files into shared object, build wheel, copy files to site packages and test
    python build_install_and_test.py
    
