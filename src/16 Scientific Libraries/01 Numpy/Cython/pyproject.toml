[build-system]
requires = ["setuptools>=74.1", "cython"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
ext-modules = [
  {name = "cythonRoots", sources = ["cythonRoots.pyx"]} 
]
[project]
name = "cythonRoots"
version = "1.0.0"
requires-python = ">=3.8"
