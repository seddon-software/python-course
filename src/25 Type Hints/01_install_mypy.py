import os

# mpy should already be available at DLS
# on other systems install with:
#       sudo apt install mypy

# this installs the python interface to mypy
os.system("python -m pip install mypy --user")
